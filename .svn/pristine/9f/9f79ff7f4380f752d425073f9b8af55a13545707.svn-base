package com.lng.dao;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.JpaSpecificationExecutor;
import org.springframework.data.jpa.repository.Query;

import com.lng.pojo.SuperUser;

public interface SuperDao  extends JpaRepository<SuperUser, Object>,JpaSpecificationExecutor<SuperUser>{

	/**
	 * @description 根据条件分页获取后台管理人员列表
	 * @author wm
	 * @Version : 版本
	 * @ModifiedBy : 修改人
	 * @date  2019年12月4日 上午9:44:31
	 * @param depId 部门编号（空时不修改）
	 * @param pageable
	 * @return
	 */
	@Query("from SuperUser su where exists(select sd.id from SuperDep sd where sd.superUser.id = su.id and sd.department.id = ?1)")
	Page<SuperUser> findPageInfoByOpt(String depId,Pageable pageable);
}
